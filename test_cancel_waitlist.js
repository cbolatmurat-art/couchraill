const fetch = require('node-fetch');

const API_BASE = 'http://localhost:8080/api';
// Or we can use the local running server if we can start it. Oh, wait, the local server crashed earlier because of port 8080!
// Let's use the live one, or we can use local if I kill port 8080.
// I will just use the live one since I'm going to commit and push anyway, but wait!
// The new endpoint is in my local changes. It's not in production yet.
// I need to start a local server to test. 
// I will just commit and push, since it's exactly what I've done before.
